---
description: Upgrade the context-mode plugin
allowed-tools: mcp__context-mode__upgrade, Bash
---

Call the context-mode `upgrade` MCP tool, run the shell command it returns, and present the result as a checklist (one line per step, ✓ / ✗).
