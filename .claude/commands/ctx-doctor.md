---
description: Run the context-mode self-check
allowed-tools: mcp__context-mode__doctor, Bash
---

Call the context-mode `doctor` MCP tool, run the shell command it returns, and present the result as a checklist (one line per check, ✓ / ✗).
